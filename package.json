{
  "name": "data-studio",
  "private": true,
  "version": "0.0.1",
  "description": "Database management studio with introspection capabilities",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm -r build",
    "start": "concurrently \"pnpm --filter server start\" \"pnpm --filter web start\"",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "keywords": [
    "database",
    "management",
    "postgres",
    "studio"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@types/recharts": "^1.8.29",
    "recharts": "^2.13.3"
  }
}
